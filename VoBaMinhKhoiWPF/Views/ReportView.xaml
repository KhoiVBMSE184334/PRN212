<UserControl x:Class="VoBaMinhKhoiWPF.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <TextBlock Text="Order Reports" FontSize="18" FontWeight="Bold" Margin="0 0 0 10"/>
        <DataGrid x:Name="ReportGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order ID" Binding="{Binding OrderID}"/>
                <DataGridTextColumn Header="Date" Binding="{Binding OrderDate}"/>
                <DataGridTextColumn Header="Customer ID" Binding="{Binding CustomerID}"/>
                <DataGridTextColumn Header="Total Items" Binding="{Binding TotalItems}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>