<UserControl x:Class="VoBaMinhKhoiWPF.Views.OrderHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <TextBlock Text="Your Order History" FontSize="18" FontWeight="Bold" Margin="0 0 0 10"/>
        <DataGrid x:Name="OrderHistoryGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order ID" Binding="{Binding OrderID}"/>
                <DataGridTextColumn Header="Date" Binding="{Binding OrderDate}"/>
                <DataGridTextColumn Header="Handled By (Employee)" Binding="{Binding EmployeeID}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>