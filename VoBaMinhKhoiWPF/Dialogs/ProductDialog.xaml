<Window x:Class="VoBaMinhKhoiWPF.Dialogs.ProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Product" Height="400" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Product Name"/>
            <TextBox x:Name="txtName"/>

            <TextBlock Text="Category ID" Margin="0 10 0 0"/>
            <TextBox x:Name="txtCategoryID"/>

            <TextBlock Text="Unit Price" Margin="0 10 0 0"/>
            <TextBox x:Name="txtPrice"/>

            <TextBlock Text="Stock" Margin="0 10 0 0"/>
            <TextBox x:Name="txtStock"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Margin="0 20 0 0">
            <Button Content="Save" Width="80" Margin="10 0" Click="Save_Click"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
